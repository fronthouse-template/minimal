After editing the config.json run the following git commands:

git add .
git commit -m "Add reverse proxy configuration"
git push https://git.fronthouse.ai/[YOUR-PROJECT-NAME]

nb.
Note the token returned after the first push and use it for subsequent pushes

eg.
git push https://git.fronthouse.ai/[YOUR-PROJECT-NAME]:[TOKEN]